class_name Enums

enum AttackType {
    MELEE,
}

enum Element {
    SHARP,
}


enum OverlayState {
    MOVE_REACHABLE,
    ACTION_REACHABLE,
    ATTACKABLE,
    PLAYER_UNIT,
    ENEMY_UNIT,
}

enum ObjectiveType {
    KILL_ALL_ENEMIES = 0,
    SURVIVE_ALL_WAVES = 1,
}
