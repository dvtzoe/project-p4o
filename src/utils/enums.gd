class_name Enums

enum ActionType {
    ATTACK,
}

enum AttackType {
    MELEE,
}

enum Element {
    SHARP,
}


enum OverlayState {
    MOVE_REACHABLE,
    ACTION_REACHABLE,
    PLAYER_UNIT,
    ENEMY_UNIT,
}

enum GameState {
    MAIN_MENU,
    STORY,
    STAGE,
}