class_name Enums

enum ActionType {
    MELEE,
}

enum AttackType {
    SHARP,
}

enum OverlayState {
    MOVE_REACHABLE,
    ACTION_REACHABLE,
}

enum GameState {
    MAIN_MENU,
    STORY,
    STAGE,
}